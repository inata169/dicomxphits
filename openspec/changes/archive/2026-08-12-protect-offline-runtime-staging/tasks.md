# Tasks

## 1. Contract and approval

- [x] 1.1 Confirm the post-inventory directory-entry race at the exact PR head.
- [x] 1.2 Confirm that directory oplocks are advisory for content changes and
  that `OWNER RIGHTS` can suppress an owner's implicit `WRITE_DAC`.
- [x] 1.3 Strictly validate this OpenSpec change.
- [x] 1.4 Obtain explicit human approval of this validated proposal before
  runtime implementation.

## 2. Elevated bootstrap and protected root

- [x] 2.1 Keep the verified parent payload locks held while relaunching the
  verified stage through absolute Windows PowerShell with UAC `RunAs`.
- [x] 2.2 Fail before verifier, Windows Installer, Python, helper, or pip if
  elevation is denied, missing, or lost.
- [x] 2.3 Create or validate the exact protected Common Application Data parent
  with built-in Administrators ownership and canonical inheritable ACLs.
- [x] 2.4 Create a fresh deterministic installation-specific runtime leaf and
  reject existing, linked, or incorrectly protected content without repair.

## 3. Complete runtime exclusion boundary

- [x] 3.1 Require the expected protected owner and DACL on every runtime
  directory and file.
- [x] 3.2 Repeat the complete file and directory inventory after the protected
  ACL and authenticated file read locks are established and before the first
  Python launch.
- [x] 3.3 Preserve authenticated-source hashing, required signer checks,
  isolated base launches, offline pip, venv, and Tkinter behavior.

## 4. Regression tests and documentation

- [x] 4.1 Add synthetic bootstrap tests for UAC ordering and elevation failure.
- [x] 4.2 Add Windows ACL regressions proving a post-inventory
  `python312._pth` injection is denied and the malicious runner does not start.
- [x] 4.3 Confirm the official CPython 3.12.10 x64 and Tcl/Tk candidate remains
  accepted from protected storage.
- [x] 4.4 Update README, English and Japanese installation instructions,
  security regression checklist, and development handoff.

## 5. Completion

- [x] 5.1 Run focused checks and every public validation required by
  `AGENTS.md` with a fresh `C:\tmp` pytest basetemp.
- [x] 5.2 Promote the accepted delta, archive this completed change, and run
  strict OpenSpec validation.

# Tests

This directory contains the public synthetic and generic regression tests.

Tests must use synthetic or generic inputs only. They must not depend
on real patient data, real clinical exports, PHITS execution results, or
repository-local official distribution files.
